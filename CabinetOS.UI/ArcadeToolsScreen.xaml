<UserControl x:Class="CabinetOS.UI.ArcadeToolsScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#111" Height="598">

    <UserControl.Resources>

        <!-- Drop shadow for log panel -->
        <DropShadowEffect x:Key="DropShadowEffect"
                          Color="#000000"
                          BlurRadius="12"
                          ShadowDepth="2"
                          Opacity="0.35"/>

        <!-- Dark rounded button style (same as HomeScreen, small version) -->
        <Style x:Key="DarkRoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#222"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#111"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>


    <Grid Margin="40"
      HorizontalAlignment="Center"
      VerticalAlignment="Center">

        <!-- COLUMN LAYOUT -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- ROW LAYOUT -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP CENTERED TITLE -->
        <TextBlock Grid.Column="0"
                   Grid.ColumnSpan="3"
                   Grid.Row="0"
                   Text="ARCADE TOOLS"
                   Foreground="White"
                   FontSize="32"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

        <!-- LEFT SIDE CONTENT -->
        <StackPanel Grid.Column="0" Grid.Row="1" Grid.RowSpan="2">

            <!-- SECTION TITLE -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Scan &amp; Sort Arcade ROMs"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <Border Height="1"
                        Background="#333"
                        Margin="0,8,0,0"/>
            </StackPanel>

            <!-- START BUTTON (updated style) -->
            <Button Content="Start"
                    Style="{StaticResource DarkRoundedButton}"
                    Click="StartScanSort_Click"/>

            <!-- STATUS LABEL -->
            <TextBlock x:Name="StatusLabel"
                       Text="Idle"
                       Foreground="#CCCCCC"
                       FontSize="16"
                       Margin="0,20,0,10"/>

            <!-- PROGRESS BAR -->
            <ProgressBar x:Name="Progress"
                         Height="20"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         Background="#222"
                         Foreground="#4CAF50"
                         Margin="0,0,0,20"/>

            <!-- SUMMARY PANEL -->
            <Border x:Name="SummaryPanel"
                    Background="#1A1A1A"
                    BorderBrush="#444"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,20,0,20">

                <StackPanel>

                    <TextBlock Text="SCAN SUMMARY"
                               Foreground="White"
                               FontSize="22"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock x:Name="SummaryTotalRoms"
                               Text="Total ROMs Scanned: -"
                               Foreground="#CCCCCC"
                               FontSize="16"
                               Margin="0,5"/>

                    <TextBlock x:Name="SummaryValidSets"
                               Text="Valid Sets: -"
                               Foreground="#CCCCCC"
                               FontSize="16"
                               Margin="0,5"/>

                    <TextBlock x:Name="SummaryMissingSets"
                               Text="Missing Sets: -"
                               Foreground="#CCCCCC"
                               FontSize="16"
                               Margin="0,5"/>

                    <TextBlock x:Name="SummaryUnknownFiles"
                               Text="Unknown Files: -"
                               Foreground="#CCCCCC"
                               FontSize="16"
                               Margin="0,5"/>

                    <TextBlock x:Name="SummaryTimeTaken"
                               Text="Time Taken: -"
                               Foreground="#CCCCCC"
                               FontSize="16"
                               Margin="0,5"/>

                </StackPanel>
            </Border>

            <!-- BACK BUTTON (updated style) 
            <Button Content="Back to Main Menu"
                    Style="{StaticResource DarkRoundedButton}"
                    Click="BackToMenu_Click"/>-->

        </StackPanel>

        <!-- LOG TITLE -->
        <TextBlock Grid.Column="2"
                   Grid.Row="1"
                   Text="LOG OUTPUT"
                   Foreground="White"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- LOG OUTPUT PANEL -->
        <Border Grid.Column="2"
                Grid.Row="1"
                Background="#222"
                BorderBrush="#444"
                BorderThickness="1"
                CornerRadius="4"
                Effect="{DynamicResource DropShadowEffect}" Height="410" Margin="0,36,0,0" Grid.RowSpan="2" VerticalAlignment="Top">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogOutput"
                           Foreground="#CCCCCC"
                           FontFamily="Consolas"
                           FontSize="14"
                           TextWrapping="Wrap"
                           Margin="10"/>
            </ScrollViewer>
        </Border>

    </Grid>
</UserControl>