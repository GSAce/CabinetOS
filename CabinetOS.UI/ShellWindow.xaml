<Window x:Class="CabinetOS.UI.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CabinetOS.UI"
        Title="CabinetOS Shell"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#111"
        Width="1280"
        Height="720">

   <!--  <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CabinetOS.UI;component/Themes/CabinetTheme.xaml"/>
                <ResourceDictionary Source="/CabinetOS.UI;component/Icons/Controller/XboxIcons.Alt.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>-->

    <Grid>

        <!-- LAYOUT: SystemBar (top), ContentHost (middle), FooterBar (bottom), MainMenu overlay -->
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <!-- SYSTEM BAR -->
        <local:SystemBar x:Name="SystemBar"
                         Grid.Row="0"
                         Panel.ZIndex="999"/>

        <!-- MAIN CONTENT HOST -->
        <ContentControl x:Name="ContentHost"
                        Grid.Row="1"
                        Focusable="False"/>

        <!-- FOOTER BAR -->
        <local:FooterBar x:Name="FooterBar"
                         Grid.Row="2"
                         Panel.ZIndex="998"/>

        <!-- SLIDE-OUT MAIN MENU OVERLAY -->
        <local:MainMenuScreen x:Name="MainMenu"
                              Grid.RowSpan="2"
                              Visibility="Collapsed"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Stretch"/>

    </Grid>
</Window>