<UserControl x:Class="CabinetOS.App.Views.SetBuilder.SetBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CabinetOS.App.ViewModels.SetBuilder"
             mc:Ignorable="d"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch"
             Background="{StaticResource CabinetBackgroundBrush}">

    <UserControl.DataContext>
        <vm:CabinetOSSetBuilderViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Source Folder:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="400" Text="{Binding Options.SourceFolder, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Output Folder:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="400" Text="{Binding Options.OutputFolder, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Build Mode:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="200" SelectedItem="{Binding Options.Mode}">
                            <ComboBoxItem Content="Merged"/>
                            <ComboBoxItem Content="Split"/>
                            <ComboBoxItem Content="NonMerged"/>
                            <ComboBoxItem Content="Salvage"/>
                        </ComboBox>
                    </StackPanel>

                    <CheckBox Content="Enable Parallel Build" IsChecked="{Binding Options.Parallel}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,16,0,0">
                    <TextBlock Text="{Binding CurrentGame}" FontWeight="Bold" Margin="0,0,0,4"/>
                    <ProgressBar Minimum="0" Maximum="{Binding TotalGames}" Value="{Binding CurrentGameIndex}" Height="20"/>

                    <TextBlock Text="{Binding CurrentRom}" Margin="0,8,0,2"/>
                    <ProgressBar Height="10" IsIndeterminate="True"/>

                    <TextBlock Text="{Binding CurrentChd}" Margin="0,8,0,2"/>
                    <ProgressBar Height="10" IsIndeterminate="True"/>

                    <TextBlock Text="{Binding CurrentSalvage}" Margin="0,8,0,0" Foreground="OrangeRed"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Start Build" Width="120" Margin="0,0,8,0" Command="{Binding StartBuildCommand}"/>
                    <Button Content="Cancel" Width="120" Command="{Binding CancelBuildCommand}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <GridSplitter Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ShowsPreview="True"/>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="False">
                <TextBlock Text="Build Log" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <ToggleButton x:Name="LogToggle" Content="Show/Hide" IsChecked="True" VerticalAlignment="Center" Padding="8,2" DockPanel.Dock="Right"/>
            </DockPanel>

            <Expander Grid.Row="1"
                      IsExpanded="{Binding IsChecked, ElementName=LogToggle}"
                      Background="{StaticResource CabinetPanelBrush}">
                <Border BorderThickness="1" Margin="0,8,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding LogEntries}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontFamily="Consolas"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Expander>
        </Grid>

    </Grid>
</UserControl>